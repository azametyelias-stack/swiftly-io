// The modal slot renders nothing unless an intercepted `/transactions/...`
// route is active (parallel + intercepting routes — SCREEN-8/9 overlay).
export default function ModalDefault() {
  return null;
}
